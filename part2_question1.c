#include <stdio.h>
#include <stdlib.h>

int my_strcmp (char arr[], char btr[]) {
	int i;
	for ( i = 0; arr[i] != '\0' && btr[i] != '\0'; i++;) {
			if (arr[i] !=tr[i]) {
				return 0;
			}
			else{
				return 1;
			}
		}
	}
}
int my_strncmp (char arr[], char btr[]) {
	for ()
	else {
		return 1;
	}
}
int my_strcpy (char arr[], char btr[]) {
	if (arr[j] = btr [j]]) {
		return arr[j]
	}

